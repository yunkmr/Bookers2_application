<%= form_with model:[book, book_comment], url:book_book_comments_path(book), remote: true do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, rows:5, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit '送信' %>
  </div>
<% end %>